# Exam-Mark-Management-System
Python app predicting student progression outcomes using credit inputs. It validates inputs, computes results (Progress, Trailer, Retriever, Exclude), displays a histogram via graphics.py, and saves data to a file.
